services:
  semweb:
    build: .
    container_name: semweb
    ports:
      - $PORT:80
    image: lucas42/semweb
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://127.0.0.1/"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
